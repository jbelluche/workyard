version: 1

workers:
  workyard-pi:
    ssh:
      user: dev
      host: workyard-pi

    register: true

    workyard:
      install: true
      daemon: true

    tailscale:
      requireConnected: true

    checks:
      doctor: true
